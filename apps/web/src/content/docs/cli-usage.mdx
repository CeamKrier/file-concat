# CLI Usage

Use the CLI to generate outputs from your terminal.

## Install

```
pnpm add -g fileconcat
```

## Example

```
fileconcat ./src --include "**/*.ts" --ignore "**/*.test.ts"
```

## Tips

- Use the same include/ignore patterns as the web app.
- Redirect output to a file for long prompts.
