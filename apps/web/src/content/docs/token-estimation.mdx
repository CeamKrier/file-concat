# Token Estimation

FileConcat estimates token usage so you can stay within your model's context window.

## How It Works

Tokens are calculated using the same tokenizer as popular LLMs. The estimate updates in real time as you add or remove files.

## Tips

- Keep an eye on the token meter while filtering.
- Split very large projects into multiple outputs.
- Use multiple files output when you exceed limits.
