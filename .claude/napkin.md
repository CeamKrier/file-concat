# Napkin

## Corrections

| Date       | Source | What Went Wrong                                                                 | What To Do Instead                                                                          |
| ---------- | ------ | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 2026-02-08 | self   | Typecheck failed in Bitbucket adapter due to implicit any on API response data. | Add explicit response payload typing when calling `response.json()` for adapter API shapes. |
| 2026-06-12 | self   | Used `fc.stringOf(fc.constantFrom(...))` from fast-check, got `default.stringOf is not a function`. | fast-check v4 removed `stringOf`. Use `fc.string({ minLength, maxLength, unit: fc.constantFrom(...) })` instead. |
| 2026-06-12 | self   | Dropped `@` Vite/TS alias after `grep "from [\"']@/"` returned zero, missed a dynamic `import("@/components/...")` in `file-viewer-content.tsx`. Dev + build both failed. | When removing an alias, grep ALL string-literal forms: `from "@/`, `import("@/`, `require("@/`, and `"@/` as a bare string. Static-import grep alone is insufficient. |
| 2026-06-12 | self   | Ran `git add <three specific paths>` then `git commit`; commit ended up with FOUR files (a stale WIP `apps/web/src/data/models.json` change leaked in). No hooks, no aliases, no `commit.all` config — root cause never identified. | Before every commit, run `git diff --cached --stat` (or `git status` after `git add`) and verify the staged set matches the named paths exactly. Catch leaks before `git commit`, not after. |
| 2026-06-12 | self   | Tried `parseOfficeAsync(filePath)` (officeparser v6 API). v7.2+ removed it; v7 returns an AST not a string. | Use `convert(file, "text")` from officeparser v7+. Returns `{ value: string, messages: OfficeIssue[] }`. `.value` is the plain text. |
| 2026-06-12 | self   | vitest `include: ["tests/**/*.test.ts"]` swept up `tests/fixtures/text-project/index.test.ts` (a stub fixture, not a real test) and the runner errored "No test suite found". | When fixtures live under `tests/fixtures/` and may carry `.test.ts` filenames, scope vitest include to `tests/*.test.ts` (top-level only) or otherwise exclude `tests/fixtures/**` so fixture files don't get parsed as suites. |
| 2026-06-12 | npm    | `npm publish` rejected the new unscoped name `file-concat` with 403 "Package name too similar to existing package fileconcat". npm's similarity guard catches typosquat-shaped names against any pre-existing slot, even with a hyphen. | For first-publish of a brand-aligned CLI when the bare slot is taken, claim the npm org (e.g. `fileconcat` on https://www.npmjs.com/org/create — open source is free, ~5 min) and publish under `@brand/cli`. Unscoped owner of `brand` does not block the `@brand/*` scope; orgs and unscoped names are independent namespaces. |
| 2026-06-13 | self   | Ran `pnpm deploy` in `apps/web/`, got `ERR_PNPM_NOTHING_TO_DEPLOY  No project was selected for deployment`. pnpm has a built-in `deploy` command (workspace package export) that shadows any `deploy` npm script in package.json. | Invoke the npm script explicitly with `pnpm run deploy` (or rename the script to a non-reserved name like `ship` / `deploy:cf`). pnpm built-in shadowing applies to other reserved verbs too — when in doubt prefix with `run`. |
| 2026-06-22 | self   | File-viewer Edit button crashed the app: `Unrecognized extension value in extension set ([object Object]). ... multiple instances of @codemirror/state are loaded`. Two physical `@codemirror/state` copies were installed — `@uiw/react-codemirror` (+ its basic-setup) resolved to `6.6.0`, our direct `@codemirror/view`/`@codemirror/lang-*` imports resolved to `6.5.2`. `EditorView.lineWrapping`/lang extensions built against one state instance, react-codemirror's internal `EditorState.create` ran against the other → cross-instance `instanceof`/facet check fails. | Pin a single state version via root `package.json` `pnpm.overrides: { "@codemirror/state": "6.6.0" }`, then `pnpm install` and `rm -rf apps/web/node_modules/.vite` (stale pre-bundle holds the old two-copy resolution; running dev server must be restarted). Verify with `grep -c "@codemirror/state@6.5.2" pnpm-lock.yaml` == 0 and that every `@codemirror+view@*/node_modules/@codemirror/state` symlink points to one version. The orphan `6.5.2` folder left in `.pnpm` is harmless. Generic rule: any "multiple instances of X are loaded" from a CodeMirror/peer-dep'd lib means duplicate transitive versions — fix at the source with a pnpm override, not just Vite `resolve.dedupe`. |
| 2026-07-05 | self   | A `--no-gitignore` CLI test used a `build/` dir to prove the flag disables gitignore delegation, but `build` is in the curated `DEFAULT_IGNORE_PATTERNS`, so it was excluded regardless and the assertion failed for the wrong reason. | When testing that `.gitignore` delegation (or `--no-gitignore`) is the thing toggling exclusion, use a directory name NOT in `default-ignore.ts` (e.g. `private/`). The two ADR-0002 mechanisms are independent — the static default floor masks any test that relies on a colliding name. |
| 2026-07-06 | self/env | `pnpm -C packages/cli publish` failed with `npm error code EUSAGE`. pnpm 10.13 leaked its own `-C packages/cli` dir + the `publish` subcommand as extra positionals into the delegated npm call — `npm publish --ignore-scripts <tgz> packages/cli publish` — and npm 11.9 rejects more than one package-spec. (Not Socket Firewall; `SFW_BYPASS=1` made no difference. Confirmed via the `~/.npm/_logs` argv.) | Publish from INSIDE the package dir, no `-C`: `cd packages/cli && pnpm publish`. Dry-run then shows a clean `argv "publish" "--ignore-scripts" "<tgz>"` and succeeds. `@fileconcat/cli@0.2.0` shipped this way 2026-07-06. |
| 2026-07-06 | self   | Playwright probe drove `input[type=file].setInputFiles` right after `goto(domcontentloaded)`; the app stayed on the landing (the change handler wasn't wired yet) and I nearly mis-read a landing marketing string ("Images aren't text") as the result card. | The SSR HTML ships the input, but React's `onChange` only attaches after hydration. Wait for hydration before driving any handler — `waitForTimeout(~3000)` or wait on a client-only marker. Also the drop-zone file inputs are `class="hidden"`, so `waitForSelector` needs `state:"attached"` (setInputFiles works on hidden inputs). Dev server: `pnpm dev` picks 5174 when a stale 5173 is up — probe the fresh port. |
| 2026-06-13 | self   | Reported "pnpm check temiz" after building a fresh slim SSR worker. The user pointed at `use-output-generation.ts:135` `setMaxFileSizeMB(32)` — an undefined identifier (real TS2304). My `pnpm check` had been a no-op for apps/web the entire session: `apps/web/tsconfig.json` is a project-references shell with `"files": []` and `references: [tsconfig.app.json, tsconfig.node.json]`. Bare `tsc --noEmit` from that directory checks nothing. | Before declaring a typecheck green, **look at the tsconfig**. If it has `"files": []` and `references`, the bare invocation is a silent pass — switch to `tsc --noEmit -p <referenced>.json` (or `tsc -b --noEmit` if every referenced config is `composite: true`). The fixed `check` script in `apps/web/package.json` is `tsc --noEmit -p tsconfig.app.json && tsc --noEmit -p tsconfig.node.json`. |

## User Preferences

- This repo's `.claude/settings.json` sets `attribution.commit: ""` and `attribution.pr: ""` — the documented Claude Code way to suppress the `Co-Authored-By: Claude ...` trailer and the "Generated with Claude Code" footer. **Honour it: never add either to commits or PRs in this repo.** If a session started before that file existed, the runtime won't have applied the override — manually omit the trailer/footer regardless.
- **Before any UI design task in this repo, invoke the `impeccable` skill.** New surface, redesign, layout shift, visual polish, component shaping — all qualify. Pure-logic edits / backend / adapters / tests / scripts do not. When unsure, err on invoking. The brand bans (no em dashes, no twin card grids, no per-section eyebrows, no 1px+ colored side-stripes) make drift expensive to walk back.

## Patterns That Work

- For SSR-safe browser-only state, guard `useState(initialiser)` and any helper called from a state initialiser with `if (typeof window === "undefined") return <fallback>`. `useEffect` callbacks already only run on the client, so localStorage reads inside an effect are fine without the guard.
- For TanStack Start in apps/web (with `@cloudflare/vite-plugin` v1.22+), the SSR build outputs to `apps/web/dist/server/index.js` and static assets to `apps/web/dist/client/`. The SSR bundle is a **Cloudflare Worker module**, NOT a Node-runnable ESM entry — running it with `node dist/server/index.js` throws `Cannot read properties of undefined (reading 'compatibilityFlags')` because `globalThis.Cloudflare` only exists inside workerd. To run the built artifact locally, use `pnpm start` (= `wrangler dev --config dist/server/wrangler.json`), which spins up workerd locally with the generated config and the prebuilt assets. The `.output/` convention is from older TanStack Start versions and no longer applies — keep both `dist/` and `.output/` in `.gitignore`.
- **Live-reactive filter via validations cache + override layer + derived effect.** For an interactive surface where ingestion-time validation is expensive but pattern matching is cheap: build `validations: Record<path, ValidationRecord>` once at ingest (binary/size check), keep `userToggled: Record<path, "include"|"exclude">` as a sticky override map, then a single `useEffect` deriving `fileStatuses.included` from `(validations, patterns, userToggled)` whenever any dependency changes. Manual toggles write to the override map (not directly to status), so pattern edits never wipe a user's explicit decision. Lives in `apps/web/src/app.tsx`.
- **Catalog over raw API for canonicalized lists.** `models.dev` exposes `api.json` (per-provider raw, 4757 entries with massive duplication), `models.json` (canonical metadata only, no cost), and `catalog.json` (combined: 203 canonical models + provider offerings with cost). We use `catalog.json` and `buildTextModelsFromCatalog` in `@fileconcat/core`. Per-canonical-model we pick the cheapest priced provider; the `FilteredModel.uid` is the catalog id (`lab/model-id`) so it stays stable when the cheapest provider shifts across deploys.
- **`Omit<Type, "field">` to express server-vs-client payload divergence.** `ModelsRegistry.providers` is optional and dropped from both the bundled `models.json` and the `/api/models` response because the runtime never reads it. Avoids 4MB+ of unread JSON in every page load.
- **`import.meta.env.SSR` is the canonical Vite DCE guard for keeping a dependency out of the SSR worker.** Pattern: `if (import.meta.env.SSR) { return Promise.resolve(); } const m = await import("./client-only-thing");`. Vite replaces `import.meta.env.SSR` with a literal `true`/`false` at build time, then rollup DCE drops the unreachable branch and the dynamic import never enters the SSR bundle. `typeof window === "undefined"` is a runtime check and does NOT trigger DCE. Lives in `apps/web/src/lib/tokens.ts` as a facade in front of `tokens-client.ts`.

## Measured Findings

- **`removeEmptyLines` was a token-cost INCREASE, not decrease.** Measured 2026-06-07 with tiktoken cl100k_base across 8 representative repo files: total +5.93% (+1047 tokens). Zero files showed savings. CRLF endings + the `^\s*[\r\n]/gm` regex appear to interact badly with the tokenizer. Feature removed end-to-end in this commit. Do not reintroduce a "remove blank lines" toggle without re-measuring against CRLF input.
- **`addLineNumbers` costs ~+44.83% tokens.** Expected, this is a navigation aid not a saving. UI now labels it accordingly in `filter-rail.tsx`.

## Known Flakes

- **`tests/dedup.test.ts` `canonicalModelKey — properties / is idempotent`** (`tests/dedup.test.ts:71`) is a fast-check property test that occasionally finds a counterexample under some seeds when the test runs alongside the rest of the core suite. Repro: appears as `Property failed by returning false` in a one-in-N full-suite run; clean immediately on re-run and clean when the file is run in isolation. Not caused by adapter / hook / sitemap changes. If it surfaces, re-run before chasing it.

## Patterns That Don't Work

- `minimatch(fullPath, "node_modules", { dot: true })` does NOT match `"node_modules/react/index.js"`. minimatch matches the entire path against the pattern; bare directory names match only the exact string. To honour bare-name ignore patterns, also test each path segment individually (`path.split("/").some(seg => minimatch(seg, pattern))`).
- Vite's `import.meta.glob("~/some/path/*.mdx")` does NOT resolve the `~` (or any) path alias in the glob pattern — it returns an empty record and every keyed lookup fails. Use a relative path (`"../../content/foo/*.mdx"`) or an absolute path (`"/src/content/foo/*.mdx"`). The keys returned are in the same form you wrote the glob in, so the lookup string must match.
- Calling browser-only APIs (`localStorage`, `window`, `document`, `matchMedia`) at module top level OR inside a `useState(initialiser)` will crash SSR with `ReferenceError: X is not defined`. Always guard.
- Vitest's `environment: "jsdom"` does NOT give you a fully working `localStorage` under Node 22+ — Node's experimental `--localstorage-file` builtin shadows it as a stub that lacks `.clear()`. Fix in test setup: `Object.defineProperty(window, "localStorage", { configurable: true, value: new MemoryStorageImpl() })` (and mirror onto `globalThis`). Same setup file must polyfill `window.matchMedia` unconditionally — jsdom does not provide one, and any `useEffect` that calls it on mount will throw in tests.
- `minimatch(path, "src/**/*", { matchBase: true })` does NOT match `"myrepo/src/app.tsx"` — `matchBase` is ignored when the pattern contains `/`, and a bare directory name does not match the same name sitting deeper in the path. **Fixed 2026-06-07** via `packages/core/src/path-utils/glob-match.ts`: `pathMatches(filePath, pattern)` handles "match anywhere" semantics — bare patterns match any segment (plus `matchBase` for basename globs); slashed patterns try a full match plus every suffix obtained by stripping leading dir components. `matchesAnyPattern(filePath, list)` consumes the comma-separated form persisted in `UserConfig.{includePatterns,ignorePatterns}`. Both `isIgnoredDirectory` and `isExcludedPath` in `apps/web/src/app.tsx` now route through these helpers — do not reintroduce inline `minimatch(...)` for user-supplied patterns.
- **Radix `ScrollArea` defeats `truncate` on its children.** The viewport wraps content in an internal `<div style="display:table; min-width:100%">`. `display:table` shrink-wraps to `max-content`, so a row wider than the popover makes that wrapper grow past the popover width; the viewport's `overflow-x:hidden` then *hard-clips* it (no ellipsis, no scrollbar) and any `truncate` child renders at full natural width — pushing trailing content (e.g. the ctx/price in the model row) off-screen. Confirmed 2026-06-22 via Playwright: a 75-char name left `nameSpan.clientWidth === scrollWidth === 560px`, `ellipsisActive=false`, row 693px inside a 378px popover. Fix: for a list that needs per-row `truncate`, do NOT wrap it in `ScrollArea`; use a native `<div className="h-[...] overflow-y-auto overflow-x-hidden">`. The native block constrains children to the popover width so flex `min-w-0` + `truncate` engage (`ellipsisActive=true`, row holds at popover width). Also aligns with the product-register guidance against reinvented scrollbars. Lives in `apps/web/src/components/model-selector.tsx`.
- **`manualChunks: { foo: ["wasm-using-package"] }` combined with `vite-plugin-wasm` leaves an orphan stub in the SSR bundle even after DCE removes all real uses of the package.** The stub is whittled down to a single `import "./X_bg.wasm"` side-effect, which is enough for wrangler to attach the wasm as `compiled-wasm` and count it against the worker size limit. Even if `import.meta.env.SSR` DCE removes every JS reference to the package, the forced `manualChunks` entry recreates the side-effect-only chunk. Fix: drop the package from `manualChunks` entirely and let vite split it naturally on the client; if no client-reachable code imports it the chunk simply isn't emitted. Seen 2026-06-13 with `@dqbd/tiktoken` blocking the 3 MiB Workers free-tier limit.

## Domain Notes

- **Blog rich-element MDX system shipped (2026-07-11, step 1 of blog build).** Four MDX components under `apps/web/src/components/blog/`: `Callout` (thin wrapper over `InfoCard` — tip→go/green, warn→info/amber, note→neutral/blue), `Steps`/`Step` (vertical numbered stepper, reuses the legal-page numbered-flow language; auto-numbers via `Children.toArray().filter(isValidElement)`), `BeforeAfter`/`Before`/`After` (two `MockWindow` frames + connector, reuses legal-page WorkedExample), and `TryIt` (the flagship). **Provider split:** the shared prose element map was moved out of `mdx-provider.tsx` into `mdx-components.tsx` as `baseMdxComponents` (a component file that also exports a const trips `react-refresh/only-export-components` — always give shared consts their own module). Docs keep `MDXProviderWrapper` (prose only); blog uses `blog-mdx-provider.tsx` `BlogMDXProviderWrapper` = `{...baseMdxComponents, Callout, Steps, Step, BeforeAfter, Before, After, TryIt}`. Custom capitalized components registered in the provider `components` map resolve in `.mdx` **without an import** (that's how `@mdx-js/react` + `providerImportSource` works). `TryIt` is `React.lazy(() => import("./try-it"))` + `Suspense` so its engine never enters the docs bundle or the article's first SSR chunk (build confirms a standalone `try-it-*.js` ~9.4kB chunk). Draft fixture `content/blog/combine-files-for-llm.mdx` (draft:true, dev-only) exercises all four in context and doubles as the Post 1 scaffold. **Authoring gotchas:** (1) `<Before>`/`<After>` code bodies MUST be a JS string expression `{`line1\nline2`}` — raw text lines get parsed as markdown and wrapped in `<p>`. (2) Internal doc links must point to REAL slugs in `content/docs/` — verified 2026-07-11 via `ls content/docs/`: `introduction`, `quick-start`, `file-filtering`, `filter-precedence`, `token-estimation`, `token-costs`, `configuration`, `cli-usage`, `github-import`, `gitlab-import`, `bitbucket-import`. **CORRECTION: `quick-start` DOES exist now** (an earlier napkin note said it did not — that is stale; re-`ls` before trusting any slug list). `token-costs` is the cost-angle doc (relevant to Post 3). Persona routes `/for/legal` and `/for/researchers` also exist (`routes/for/`). (3) Publishing a post = flip `draft:false`; the sitemap auto-includes it and the footer Blog link ungates via `hasPublishedPosts()`. (4) **Frontmatter `title` IS the on-page `<h1>`** (route `blog/$slug.tsx` renders it in the article header) AND the SERP `<title>` (`${title} - FileConcat`). So the MDX body must start at `##`, never `#`; and a keyword-doc that specs a separate SERP-Title vs on-page-H1 must be COLLAPSED into one `title` value (pick the one matching the primary keyword that still fits the ~60-char `<title>` budget). (5) Browser-side PDF/DOCX/Office extraction is real: `use-file-ingestion.ts` → `extractDocumentText` (`lib/extract-document.ts` facade → `extract-document-client.ts` → core `extractable-document.ts`); empty result → "No extractable text" (scanned/image PDFs, no OCR in v1), throw → "Couldn't extract text". `ValidationRecord.extracted` flows to the result view's `extractedFiles`. So `TryIt` genuinely extracts a dropped PDF — Post 3 can demo and claim this honestly. **All three posts now shipped** (Post 3 `convert-pdf-to-text-for-llm.mdx`, commit e9688f8, 2026-07-11); the blog build sequence steps 0-3 are done. `EXTRACTABLE_DOCUMENT_EXTENSIONS` (core) = pdf/docx/xlsx/pptx/odt/ods/odp; empty extraction surfaces "No extractable text" (no OCR, no silent drop). Next initiative work is the deferred comparison post, not more of the 3-post slate.
- **`TryIt` is a real inline mini-tool, NOT an AppFlow embed. `AppFlow` (`components/app/app-flow.tsx`) is a full-page state machine** — it renders `<div className="min-h-screen">` with its own `TopBar`, `SiteFooter`, and a fixed `SettingsDrawer`, so it CANNOT be dropped inline in an article (the persona pages' `renderLanding` slot only works because AppFlow *is* their whole page). `TryIt` instead composes the same hooks directly: `useFileIngestion(DEFAULT_CONFIG)` + `useFilterState({entries, validations, includePatterns:"", ignorePatterns: DEFAULT_IGNORE_STRING})` + `estimateTokenCount`/`preloadTokenEstimator` + core `assembleOutput`/`generateFileTree`/`generateProjectName`/`summarizeExclusions` + the real `DropZone`, with a local `idle→processing→result` phase. Contained panels (`rounded-panel border-border-strong bg-surface-alt`), no nested cards. Verified in-browser: drop 3 files → "N files combined · ≈T tokens" + Copy/Start-over, desktop + mobile. **The result card no longer links "Open the full tool" (removed 2026-07-11, commit be6bdc6).** Why: that link was `<Link to="/">`, a full route change that mounts a fresh `AppFlow` at `/` — and `AppFlow` starts at `phase:"landing"` with its OWN `useFileIngestion`/`useFilterState` and NO prop/store seam to receive the inline state, so clicking it silently discarded the reader's dropped files. `/app` also just `redirect({to:"/"})`s, so there is no separate full-tool route to hand state to either. Options were (A) build a handoff store + seed `useFileIngestion` with pre-decoded entries + reconcile TryIt's demo config vs the full tool's `useConfig`, or (B) drop the link. Chose B (brand restraint + the `BlogShell` header already carries an "Open the tool" fresh-start entry point). If a future task wants continuity, that's option A and it's a real feature, not a prose tweak. Result-card copy was also softened from the imperative "Use the full tool…" to "The full tool lets you…" so nothing implies a button that isn't there.
- **Two different `DEFAULT_CONFIG`s — do not confuse them.** Core's `DEFAULT_CONFIG` (`@fileconcat/core`, type `ProcessingConfig`) is the **ingestion** config: has `maxFileSizeMB`, `excludeHiddenFiles`, etc., but **NOT** `includePatterns`/`ignorePatterns`/`outputStyle`. Those three live on the web **`UserConfig`** whose default is a private const in `hooks/use-config.ts` (returned from `useConfig()`, not exported). Reading `DEFAULT_CONFIG.includePatterns` off the core one is a TS2339 that only surfaces at `pnpm check`. For default filtering outside the hook, use `""` + `DEFAULT_IGNORE_STRING` (both from core) and `outputStyle:"xml"`.
- **Browser-verify quirk (2026-07-11):** repo has `playwright-core@1.61.1` but its bundled chromium (1228) is NOT installed; launch with `executablePath: ~/.cache/ms-playwright/chromium-1208/chrome-linux64/chrome`. Import via the `.pnpm/playwright-core@1.61.1/.../index.mjs` absolute path. Pre-existing console noise on every route: a `dangerouslySetInnerHTML` hydration mismatch warning from the Clarity analytics + schema.org JSON-LD `<script>` in `__root.tsx` — filter it out when scanning for real errors; it is not from app components. **The `DropZone` renders TWO hidden file inputs** — a folder-picker (`webkitdirectory`/`directory`) and a plain multi-file input. Driving `.setInputFiles(<a file>)` on the `webkitdirectory` one throws `input requires passing a path to a directory`; target `input[type="file"]:not([webkitdirectory]):not([directory])` when dropping files (use the directory input only with a dir path). To prove a dropped PDF was actually extracted+assembled, grant `clipboard-read`, click the result-card Copy button, and assert `navigator.clipboard.readText()` contains a known string from the PDF — more definitive than reading the result heading (a crude "line matching /token/" finder grabs the BeforeAfter mockup's token chip, not the TryIt output). Confirmed 2026-07-11 verifying Post 3 with a hand-built 606-byte PDF.
- **gitignore delegation shipped (2026-07-05).** `createGitignoreMatcher(sources)` + `collectGitignoreSources(files)` in `packages/core/src/path-utils/gitignore-matcher.ts` (dep: `ignore@^7`, a **direct** core dep — pure JS, safe for the Workers bundle, tsup-bundles into the CLI). Hierarchical: one `ignore()` instance per `.gitignore` anchored to its own `dir`, composed **shallow→deep** so nested files + `!` negation override parents; the web drop-root prefix is handled by dir-scoping (a `.gitignore` at `myproj/.gitignore` has `dir:"myproj"` and only governs paths under it). CLI (`concat.ts`) honors it **by default** (`--no-gitignore` opts out) via a dedicated `glob("**/.gitignore",{dot:true})`, then filters the walk like `--exclude` — dropped, **not** counted in `skippedBreakdown`, so the `--json` schema is unchanged (no version bump). Web `use-filter-state.ts` builds the matcher from `entries` (the `.gitignore` files ride along in ingestion) and now **composes** include+ignore+gitignore. **Whitelist-exclusivity was REMOVED**: a non-empty include list no longer disables ignores — include narrows, ignore(+default noise)+gitignore always subtract, and the manual per-file toggle is the one-off escape hatch. The quick presets in `settings-drawer.tsx` already shipped an include AND an ignore list, so they depended on compose; this fixes them. gitignored files surface in the result-view noise-skipped count and stay re-includable (ADR-0002 transparency, no new UI). ADR filenames: `docs/adr/0001-content-based-multi-encoding-text-classification.md` + `0002-curated-ecosystem-aware-noise-exclusion.md` (0002 title says "noise exclusion" but its "Surface-specific implementation notes" hold the gitignore design).
- **Text/binary/ambiguous classification lives in `packages/core/src/file-processing/text-classification.ts`** (added 2026-07-05). `classifyBytes(bytes)` (pure) + `readFileAsText(file)` (File wrapper) → `DecodedText { classification: "text"|"binary"|"ambiguous", text, encoding }`. Encoding-agnostic: BOM (utf-8 / utf-16le / utf-16be) + BOM-less UTF-16 via alternating-NUL detection + printability-ratio bands (`BINARY_RATIO 0.3`, `AMBIGUOUS_RATIO 0.05`). `validateFile` delegates to it (extension-denylist fallback ONLY when bytes can't be read); ambiguous → `isValid:true` + `result.classification`. The old `isBinaryContent`/`isBinaryFile` **any-NUL sniff was removed** (see `docs/adr/0001`) — do NOT reintroduce a "any NUL byte → binary" rule; it silently drops UTF-16 source files. UTF-32 not yet handled. **Web + CLI are now wired through the classifier** (2026-07-05): `use-file-ingestion.ts` reads content via `readFileAsText`; `concat.ts` via `classifyBytes` (keeps the extension fast-skip). `ValidationRecord.classification` flows to `app-flow` `flaggedFiles` → `ResultView` amber "might not be plain text" InfoCard. See `CONTEXT.md` for the Text/Binary/Ambiguous/Noise glossary and `docs/adr/` for the two decisions.
- **Loader shows live fetch STAGES + a climbing count, not just a spinner (2026-07-06).** `FetchOptions.onStatus?: (message: string) => void` (`packages/core/src/sources/types.ts`) is a coarse stage channel every remote adapter emits on: "Connecting to X" → "Finding the default branch" → "Listing files" → "Downloading N files". Fills the long silent pre-download window (branch resolve + paginated tree). **A static stage label still reads as frozen on slow network** — so the paginated adapters (gitlab `fetchAllTreePages`, bitbucket `fetchDirectoryContents`) emit a running `Listing files… N found` per page/response, giving real movement. Verified live against gitlab-org/gitlab-runner: heading climbed 71→1371 across 14 pages. github's tree is a single request (no page to tick). Only emit the count when >0 (skip the "0 found" flash when early pages are all directories). Web: `IngestProgress.note` carries it; `ingestRepo` sets an immediate `"Connecting…"` then merges `onStatus`/`onProgress` via functional `setProgress` (don't clobber `note` from onProgress, or `done/total` from onStatus); `handleDrop` sets `"Scanning files…"` for the folder-walk gap. `app-flow` uses `progress.note` as the processing heading, falling back to the phase name. Unit-tested on gitlab (`source-adapters.test.ts` asserts listing+downloading fire). Do NOT let a repo fetch sit on a static spinner again — users read it as frozen and leave.
- **Bitbucket adapter: default-branch bug + a hard anonymous rate cap (2026-07-06).** (1) The fetch 404'd for any repo whose default branch isn't `main`: the code did `let branch = parsed.branch || "main"` then guarded the default-branch lookup behind `if (!branch)` — dead code, so the real default was never resolved. Fixed by `resolveBitbucketRef` (`bitbucket.ts`): follow the bare `/src` redirect and read the commit hash out of `response.url` (`.../src/{40-hex}/`). This ALSO handles slashed default branch names (e.g. fargo3d/public → `release/public`), which break `/src/{ref}/{path}` routing (Bitbucket reads `release` as the ref, `public/` as a path → 404). `fetchWithRateLimitRetry` returns the raw Response so `response.url` (post-redirect) is available; mock it in tests via a `url` field on `makeResponse`. (2) **Bitbucket Cloud anonymous API is ~60 req/hour per IP** — brutally low. The recursive per-directory `/src` walk + concurrent per-file `/src/{commit}/{path}` downloads (Promise.all) burn quota fast, so any non-tiny repo 429s. Confirmed live: `{302:1, 429:1}` — resolve worked, first listing got 429. Not a bug in the fix; it's the cap. `friendlyFetchError` (`app-flow.tsx`) must match `too many requests` (classifyResponseError's 429 wording), not just `429`/`rate limit`, or a throttled fetch mislabels as "make sure it's public". **Open follow-up if Bitbucket needs to handle bigger repos:** switch file downloads to `bitbucket.org/{ws}/{repo}/raw/{ref}/{path}` (may dodge the API limit like GitHub's raw host does — needs a CORS check) and/or cap download concurrency. Verify against the real API — but note testing exhausts the 60/hr quota fast; single calls return 200 while a burst 429s.
- **Result view splits excluded files into TWO honest buckets (2026-07-06).** `validateFile` returns `!isValid` for three distinct reasons: `"Binary file"`, `"Hidden file"` (dotfiles — TEXT), `"File size exceeds NMB limit"` (text, too big). `app-flow` now splits them: `notText` (binary/archive/read-error) keeps the amber "aren't text, left out" card; `skippedByDefault` (Hidden file + oversize) gets a distinct blue `neutral`/`EyeOff` "N text files left out by default" card that says they're readable + links to Adjust (re-includable via the file-tree toggle). Before, every dotfile was mislabeled "aren't text". `DEFAULT_CONFIG.excludeHiddenFiles = true`, so `.gitignore`/`.eslintrc.js`/`.env` etc. are the common occupants. `noiseSkipped` stat left as pattern-noise only (didn't fold hidden in — avoid regressing the stat). Brand ban caught in my own copy: I wrote an em dash in the card body — replaced with a colon. Verified live via Playwright (4 dotfiles → correct card, 2 real files combined, no page errors).
- **Processing view is driven by REAL progress, not a scripted narration (2026-07-05).** `use-file-ingestion` exposes `progress {phase:"reading"|"fetching"|"unpacking", done, total}` (throttled to ~100 ticks); `app-flow` maps it to a heading + percent, and reveals the result the moment `run()` resolves (no padding, no min-duration). Do NOT reintroduce the old fixed-timer step checklist, the `Math.min(95,…)` percent cap, or fabricated "Skipping noise: node_modules/lockfiles/js-ts" steps — those were removed because they lied about what was in the drop and stuck at 95%. Remote adapter `onProgress` is consumed (was `() => {}`).
- **File-viewer editor highlighting (`apps/web/src/components/lazy-editor-codemirror.tsx`) — the FULL fix (2026-06-22).** Symptom: editor rendered flat, uncolored text in dark mode; tweaking the @uiw theme/highlight-precedence did NOTHING. Four independent root causes, all required:
  1. **`language` was never passed to `LazyEditor`** (`file-viewer-content.tsx` called `<LazyEditor value onChange/>` with no `language`). No language → no parse tree → nothing to highlight. Fix: `language={getLanguageFromPath(path)}` (helper in `@fileconcat/core`).
  2. **Broken language-id matching.** `getLanguageFromPath` returns `"typescript"`/`"javascript"`/`"markdown"`, but `"typescript".includes("ts")` is FALSE (no adjacent "ts") and `"javascript".includes("js")` is FALSE. Match whole ids against an allow-list, never substrings.
  3. **Duplicate `@codemirror/view` (6.38.8 + 6.43.1) and `@lezer/common` (1.3.0 + 1.5.2).** Different `EditorView` classes → `oneDark`'s `EditorView.theme` + the highlight decoration facet are FOREIGN to the editor's view instance → bg+tokens silently don't apply (while `EditorView.lineWrapping` from the matching copy DID apply — that asymmetry is the tell). Different `@lezer/common` → parse-tree node types don't match the highlighter. Fix: pin singles via root `pnpm.overrides`: `@codemirror/state:6.6.0`, `@codemirror/view:6.43.1`, `@codemirror/commands:6.10.3`, `@lezer/common:1.5.2`.
  4. **Vite optimizer inlined separate copies** of `@lezer/highlight`/`@lezer/common`/cores into each pre-bundled chunk → tag identities mismatch across chunks → `highlightTree` matches nothing. Pure-node test of the same packages worked (single require cache), browser didn't — that isolates it to the optimizer. Fix: list the shared cores in `optimizeDeps.include` (`@codemirror/state|view|language`, `@lezer/common|highlight|lr`) so each becomes one shared chunk. **`optimizeDeps.include` entries must resolve as DIRECT deps** under strict pnpm — had to add `@codemirror/state`, `@lezer/common|highlight|lr` (plus earlier `@codemirror/language`, `@codemirror/theme-one-dark`) to `apps/web` deps or Vite errors "Failed to resolve dependency … present in optimizeDeps.include".
  Editor config: `theme="none"` + `basicSetup={{ syntaxHighlighting:false }}`, own the highlight explicitly — append `oneDark` (dark) or `syntaxHighlighting(defaultHighlightStyle)` (light) LAST; `isDark` read from `document.documentElement.classList.contains("dark")` + MutationObserver (NOT `useTheme()` context). `markdown()` needs `codeLanguages` to color fenced blocks; we map js/ts/json→`jsLanguage.language`, rest stay plain (no lazy grammar fetch — offline brand).
  **Verify any CodeMirror change with a real browser probe** (Playwright headless, chromium at `~/.cache/ms-playwright/chromium-1193/...`): count `.cm-content .cm-line span`, read `.cm-editor` bg, sample token colors. Static reasoning about CM facet/instance issues is unreliable — I shipped a no-op "fix" twice before measuring. `/tmp/pw` had `playwright-core` + the probe scripts this session.
- ESLint config (`eslint.config.js`) is minimal — no `argsIgnorePattern` for `_`-prefixed names. So `const _foo = bar()` STILL trips `@typescript-eslint/no-unused-vars`. Either delete the assignment or actually consume it; the underscore prefix is not a ts-eslint escape hatch in this repo.
- Catch-clauses without a binding (`catch { ... }`) are the right way to silence unused-`error` lint, since the rule applies to caught errors here.
- `addLineNumbers` in `packages/core/src/file-processing/transform.ts` must drop a trailing empty element after `split("\n")` so a final newline doesn't produce a phantom numbered line. Tests in `tests/file-processing.test.ts` rely on this.
- **`/app` workbench architecture (post 2026-06-07 redesign):** three-region grid — top `SourceBar` with project + counts + foreground "Start over"; left `FilterRail` (lg+) or Radix Dialog drawer (<lg) holding preset chips + include/ignore textareas + counter + Advanced disclosure; main column with `FileTree`, optional `TokenSection`; bottom-sticky `ActionBar` carrying token summary + inline property chips (Format pill → auto/single/multi popover, inline chunk-size `−/+` stepper shown only when multi, Style pill → xml/markdown popover) + Copy (single only) + Download. The single "Options" popover button was removed 2026-06-22 in favour of the per-property chips. Format/style/chunk now persist via `useConfig` — `UserConfig` is v6: `defaultOutputFormat` is `"auto"|"single"|"multi"` (auto defers to the live token recommendation; the v5→v6 migration resets the dead pre-v6 value to `auto`), plus `chunkSizeKB`. State ownership stays in `app.tsx`. `WorkbenchHeader` + `ConfigPanel` + `PreviewModal` (was dead) are gone.
- **Drop-time vs filter-time pattern split:** at directory walk, only the hardcoded `HARDCODED_PRUNE_DIRS = {".git", "node_modules"}` set is excluded (browser-crash safety). Every other ignore / include pattern is purely post-ingestion via the live `useEffect`. Preset chip clicks therefore re-shape the already-dropped set instantly. Cost: a drop containing `dist/`, `build/`, `vendor/`, lock files etc. pulls them into memory before patterns hide them. Acceptable for typical projects; audit if drops feel slow.
- **Model selector data path:** `apps/web/scripts/fetch-models.ts` (prebuild) + `apps/web/src/routes/api/models.ts` (edge) both call `https://models.dev/catalog.json` and route through `buildTextModelsFromCatalog(catalog)`. Output sorted by `release_date` desc. `useModels` hook re-applies the same sort after `dedupAndPruneModels` (runtime defense for stale caches). The bundled `models.json` lives at `apps/web/src/data/models.json`.
- **Brand bans active for this repo:** no em dashes, no twin card grids, no per-section eyebrows, no 1px+ colored side-stripes. PRODUCT register applies to `/app`; BRAND register applies to landing, `/about`, `/docs`. PRODUCT.md anchors to BRAND register overall.
- **Adapter literal contract (post 2026-06-12 review):** every `*Adapter` literal is `{ type, meta, matches, parseUrl, fetchFiles, priority? }`. `parseUrl`/`fetchFiles` point directly at the free function — no `(url) => fooParse(url)` wrappers. `urlAdapter` carries `priority: "fallback"`; the registry consults specific adapters first regardless of insertion order, so adapter ordering in `default-registry.ts` is presentational only.
- **Shared progress reporter:** all source adapters route progress through `createProgressReporter({ totalFiles, totalBytes?, onProgress })` in `packages/core/src/sources/progress.ts`. github passes `totalBytes` and calls `bytesReceived` during streaming reads. gitlab/bitbucket omit `totalBytes` and only call `fileComplete`. Do not reintroduce inline `updateProgress` closures or the `downloadedBytes: completedFiles` lie.
- **Canonical pattern matcher:** `pathMatches(filePath, pattern)` in `packages/core/src/path-utils/glob-match.ts` is the only place "match-anywhere" semantics live. `shouldSkipPath` is a thin wrapper that runs the default ignore list through it. Do not write fresh `minimatch(...)` + segment-loop combinations elsewhere.
- **Shared DataTransfer walker:** `apps/web/src/lib/collect-from-drop.ts` (`collectFromDataTransfer`) is the only `FileSystemEntry` walker in the web app. Returns `{ collected, failed }`. Used by `useFileIngestion.handleDrop` and `components/landing/hero.tsx`. The workbench passes `skipDir: (n) => HARDCODED_PRUNE_DIRS.has(n)`; the landing passes no skipDir (it stages whatever you drop).
- **Landing page layout:** `routes/index.tsx` is a tiny composer (~45 LOC). Folds live under `apps/web/src/components/landing/`: `hero.tsx` (Hero + DropZone + StagedSummary), `output-preview.tsx`, `marketing-folds.tsx` (Audience + PrivacyFold + CLIFold). Adding a new fold means a new component file + an import in `routes/index.tsx`.
- **FileTree decomposition:** `apps/web/src/components/file-tree/` is the directory now. `tree-data.ts` holds the pure helpers (`buildFileTree`, `calculateInclusionState`, `collectFileIndices`, `collectDirectoryPaths`); `tree-node-row.tsx` is the recursive row component; `index.tsx` is the orchestration shell. The pure helpers are test-friendly without a React DOM. Imports stay at `~/components/file-tree`.
- **`FilteredModel.uid` is the canonical id (no `id` field).** Use `model.uid` everywhere — in React keys, localStorage keys, fuzzy-search targets, and the `migrateLegacyUid` lookup. Previous `id` field was dropped because it was always `=== uid`.
- **Token estimator self-caps input size.** `apps/web/src/lib/tokens.ts` `estimateTokenCount` falls back to `length / 4` for inputs over 1 MB. Do not reintroduce `MAX_INLINE_BYTES` / `MAX_TOKEN_ESTIMATE_BYTES` constants at call sites; just call `estimateTokenCount(text)` and trust the cap.
- **CLI publish identity.** Package name on npm is `@fileconcat/cli` (the unscoped `fileconcat` slot is owned by another author, and npm's similarity guard also rejects `file-concat`). Bin command is `file-concat` (independent of the npm name). Lives at `packages/cli/`. `@fileconcat/core` is `private: true` and bundled into the CLI dist via `tsup` `noExternal`, so the CLI tarball has no `@fileconcat/core` runtime dependency. Never move `@fileconcat/core` back into the CLI's `dependencies` field — `@fileconcat/core` is unpublished and install would fail. **Published version: `0.2.0`** (2026-07-06; adds content classification + `.gitignore` honoring over 0.1.0). The version is now a SINGLE source of truth — `src/index.ts` does `import pkg from "../package.json"` → `.version(pkg.version)` (tsup inlines it) and the `--version` test asserts `pkg.version`. A bump = edit `packages/cli/package.json` only. Publish with `cd packages/cli && pnpm publish` (NOT `pnpm -C ... publish` — see corrections).
- **CLI harness contract (post 2026-06-12 publish prep).** Stdout = artifact (concatenated content with `--stdout`, or JSON summary with `--json`). Stderr = chatter (progress lines via the stderr-routed logger, warnings, errors). `--stdout` and `--json` are mutually exclusive (exit 1). `--quiet` silences stderr info but keeps errors. `--parse [formats]` opt-in (default OFF) routes PDF/DOCX/XLSX/PPTX/ODT/ODS/ODP through `officeparser.convert(file, "text")`. Failed parses increment `skippedBreakdown.parseFailed` and continue (no fatal). JSON summary schema is documented in `packages/cli/README.md` "For AI coding agents"; harness scripts may depend on it — version bump if you change the keys.
- **CF Workers free-tier worker size limit is 3 MiB gzipped (paid tier 10 MiB).** Wrangler shows raw KiB per attached module in its deploy table; the limit check is on the gzipped total reported as `Total Upload`. Wrangler walks the import graph from `wrangler.json` `main` (the generated `dist/server/wrangler.json`) and attaches every transitively reachable module, INCLUDING those behind `React.lazy` / dynamic `import()` — those static reference forms are sufficient for inclusion. The only reliable way to drop a dep from the worker is to make it unreachable in the SSR graph at the source level (`import.meta.env.SSR` DCE guard) AND drop any `manualChunks` entry that force-emits the chunk.
- **`@cloudflare/vite-plugin@1.22.0` breaks the Vite 6 plugin `buildApp` hook chain.** In its `config()` it sets `builder.buildApp = userConfig.builder?.buildApp ?? createBuildApp(ctx.resolvedPluginConfig)` (dist/index.mjs ~line 21837). `createBuildApp` only does `await builder.build(env)` for each worker + client environment; it does NOT iterate any other plugin's `buildApp: { order, handler }` hook. TanStack Start's prerender runs via exactly that hook (`tanstack-start-core:post-build` plugin), so passing `pages` + `prerender: { enabled: true }` to `tanstackStart({...})` is silently a no-op in this stack — no static HTML is emitted, with or without `vite build --app` / `builder: {}`. Verified 2026-06-13 by inlining diagnostic `{ buildApp: { handler() { console.log("…") } } }` plugins — they never fire. Workarounds: (a) eliminate the symptom in source (the docs flash was fixed by switching `routes/docs/$slug.tsx` to an eager `import.meta.glob({ eager: true })` so SSR HTML already contains rendered MDX), or (b) write a post-`vite build` script that spawns `wrangler dev --config dist/server/wrangler.json`, curls each path, writes HTML to `dist/client/{path}/index.html`, then removes the matching path from `wrangler.jsonc` `assets.run_worker_first` so static asset serving wins. Until CF restores the hook chain, do not assume a TanStack Start `prerender` config is doing anything.
- **Web app vite config is `apps/web/vite.config.ts`** — the spine with `cloudflare()`, `tanstackStart()`, `wasm()`, `topLevelAwait()`, `react()`, `mdx()`, alias setup, optimizeDeps tiktoken exclusion, and `build.rollupOptions.output.manualChunks`. `apps/web/app.config.ts` is a near-empty TanStack Start config stub (only `react.strictMode: true`). CLAUDE.md still calls `app.config.ts` "the spine"; that's stale — update before relying on it.
- **`apps/web/src/lib/tokens.ts` is a facade, not the real estimator.** Public surface: `estimateTokenCount(text)` (synchronous, returns bytes/4 approximation until the WASM loads, then the exact count) and `preloadTokenEstimator()` (call from a client-only `useEffect` to warm the WASM and flip a state value so dependent `useMemo`s recompute). Real implementation in `apps/web/src/lib/tokens-client.ts`. Do not reintroduce a top-level `@dqbd/tiktoken` import in `tokens.ts` — that re-attaches the 5.4 MiB wasm to the Workers bundle.
- **Theme flash root cause (fixed 2026-06-13 in `routes/__root.tsx`):** the SSR HTML ships `<html>` with no theme class. The inline pre-hydration script sets `.dark` before first paint, so FCP is correct. BUT `ThemeProvider` (`theme-provider.tsx`) runs a `useEffect` that does `root.classList.remove("light","dark"); root.classList.add(theme)` on hydration. When theme resolves to `"light"`, that *adds* the `.light` class to `<html>` after the dropzone has painted. The dropzone uses `transition-[background-color,border-color,box-shadow,transform] duration-300`, so the CSS-variable jump triggered by that class addition animates as a 300ms flash (looks like white→black to dark-mode users when the symptom is the dropzone re-settling). Fix: have the inline head script append a `<style data-theme-init>` containing `*,*::before,*::after { transition-duration:0s !important; transition-delay:0s !important; animation-duration:0s !important; animation-delay:0s !important; }` BEFORE adding the theme class, then remove the style after 2 `requestAnimationFrame`s post-`load`. Suppression covers the entire init window including the post-hydration `useEffect` flip. Do not collapse the double-rAF to a single one — the first frame is the layout settle, the second is the actual paint.
- **Binary files are locked out of curation (ADR-0009, 2026-07-11).** The web `FileStatus` (core `types.ts`) now carries `classification` + `extracted`, threaded through `use-filter-state.ts` from the ingest `ValidationRecord`. A file with `classification === "binary"` is non-re-includable: `toggleFile` early-returns, `toggleMany` skips it on include sweeps, and `tree-node-row.tsx` renders a `Lock` icon with the toggle + filename-preview click disabled (`isBinary`/`canOpen`). Ingest (`use-file-ingestion.ts`) no longer decodes binary bytes — it pushes `{ path, content: "" }` so the row still appears in the tree but a force-include can never leak mojibake into the bundle. The old "manual toggle = universal escape hatch" comment is now scoped to text-bearing files only. Failed-extraction docs (classification `binary`, never pushed to `entries`) are locked by absence, not a visible row.
- **Default `ignorePatterns` is pre-populated with `DEFAULT_IGNORE_STRING`, not empty.** `use-config.ts` `DEFAULT_CONFIG` ships `includePatterns: ""`, `ignorePatterns: DEFAULT_IGNORE_STRING`. So a settings preset that means "reset / everything" must set `ignore: DEFAULT_IGNORE_STRING` (NOT `""`) — clearing it would flood node_modules/lockfiles back in. The "Everything readable" preset (`settings-drawer.tsx`) matches `DEFAULT_CONFIG` exactly, so it reads as active on first load.
- **Settings drawer IA (2026-07-11):** section order is Quick presets (grouped: General / Code stacks / Documents) → Files tree → collapsed "Advanced · ignore & include patterns" disclosure (`advancedOpen`, chevron) → Cost. The pattern textareas live inside the collapsed Advanced block so a non-dev only meets the tree; presets are static/grouped with NO persona-detection logic (labels do the differentiation). `PRESETS` was renamed `PRESET_GROUPS`.
- **Explore-agent caution (this repo):** a subagent claimed `FileStatus` already had a `classification` field and mislocated `use-filter-state.ts` under `components/file-tree/` — both wrong. The hook lives at `apps/web/src/hooks/use-filter-state.ts` and `FileStatus` gained `classification` only on 2026-07-11. Verify agent-reported field names/paths against source before building on them.
